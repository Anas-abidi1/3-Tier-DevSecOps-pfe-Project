# LAB/PRACTICE VALUES -- see mysql-secret.example.yaml for why this
# shouldn't be committed with real values in a non-lab project (this file
# exists here, with real values, because this is explicitly a lab/practice
# environment). Apply with: kubectl apply -f k8s-prod/mysql-secret.yaml
---
apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
  namespace: prod
type: Opaque
stringData:
  MYSQL_ROOT_PASSWORD: Aditya
  MYSQL_USER: crud_app_user
  MYSQL_PASSWORD: Aditya
