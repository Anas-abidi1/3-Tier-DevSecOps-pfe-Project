# backend-secret.example.yaml
#
# Template only -- do not commit with real values filled in. Create the
# real secret the same way as mysql-secret (see mysql-secret.example.yaml):
#
#   kubectl create secret generic backend-secret -n prod \
#     --from-literal=JWT_SECRET="$(node -e "console.log(require('crypto').randomBytes(48).toString('hex'))")"
#
# Shape backend.yaml expects:
#
# apiVersion: v1
# kind: Secret
# metadata:
#   name: backend-secret
#   namespace: prod
# type: Opaque
# stringData:
#   JWT_SECRET: REPLACE_ME
